---
import { categories } from '../data/categories.ts';
import CategoryIcon from '../components/CategoryIcon.astro';
const base = import.meta.env.BASE_URL ?? '';
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Japan Daily Hub | Authentic Japanese Products</title>
  <meta name="description" content="Authentic Japanese products at Japanese market prices. Direct from Tokyo — without the import markup. Ships worldwide." />
  <meta property="og:title" content="Japan Daily Hub | Authentic Japanese Products" />
  <meta property="og:description" content="Curated Japanese products at genuine local prices. The same items found in Tokyo — delivered to your door." />
  <meta property="og:image" content="https://images.unsplash.com/photo-1493976040374-85c8e12f0c0e?w=1200&q=80" />
  <meta property="og:url" content="https://japandaily-hub.github.io/japandaily-hub/" />
  <meta property="og:type" content="website" />
  <meta property="og:site_name" content="Japan Daily Hub" />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content="Japan Daily Hub | Authentic Japanese Products" />
  <meta name="twitter:description" content="Authentic Japanese products at genuine local prices. Direct from Tokyo, without the import markup." />
  <meta name="twitter:image" content="https://images.unsplash.com/photo-1493976040374-85c8e12f0c0e?w=1200&q=80" />
  <link rel="canonical" href="https://japandaily-hub.github.io/japandaily-hub/" />
  <!-- Google Analytics 4 — replace G-XXXXXXXXXX with your Measurement ID -->
  <script is:inline async src="https://www.googletagmanager.com/gtag/js?id=G-XXXXXXXXXX"></script>
  <script is:inline>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'G-XXXXXXXXXX');
  </script>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link href="https://fonts.googleapis.com/css2?family=Noto+Serif+JP:wght@300;400&family=Cormorant+Garamond:wght@300;400&display=swap" rel="stylesheet" />
  <style>
    :root {
      --ink: #12100e;
      --gold: #b8963e;
      --gold-l: #d4af5a;
      --washi: #f6f1e9;
    }
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body {
      background: var(--washi);
      color: var(--ink);
      font-family: 'Noto Serif JP', serif;
    }

    /* TICKER */
    .ticker {
      background: var(--ink);
      color: rgba(255,255,255,.6);
      font-size: .6rem;
      letter-spacing: .18em;
      padding: .4rem 1.5rem;
      display: flex;
      align-items: center;
      gap: 1rem;
      flex-wrap: wrap;
    }
    .ticker-lbl { color: var(--gold); }
    .ticker-rate { color: #fff; }
    .ticker-save { color: #4ade80; margin-left: auto; }

    /* HEADER */
    header {
      position: sticky;
      top: 0;
      z-index: 100;
      background: rgba(246,241,233,.95);
      backdrop-filter: blur(12px);
      border-bottom: 1px solid rgba(184,150,62,.25);
    }
    .header-inner {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 1.5rem;
      height: 64px;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }
    .logo {
      font-family: 'Cormorant Garamond', serif;
      font-size: 1.3rem;
      font-weight: 300;
      letter-spacing: .22em;
      text-transform: uppercase;
      color: var(--ink);
    }
    .logo em { color: var(--gold); font-style: normal; }
    .lang-select {
      background: transparent;
      border: 1px solid rgba(184,150,62,.35);
      color: var(--ink);
      padding: .35rem .7rem;
      font-size: .72rem;
      font-family: inherit;
      cursor: pointer;
      border-radius: 2px;
      outline: none;
      letter-spacing: .05em;
    }
    .lang-select:hover { border-color: var(--gold); }

    /* HERO */
    .hero {
      background: var(--ink);
      color: #fff;
      text-align: center;
      padding: 6rem 1.5rem 5rem;
      position: relative;
      overflow: hidden;
    }
    .hero-bg {
      position: absolute; inset: 0;
      background: url('https://images.unsplash.com/photo-1493976040374-85c8e12f0c0e?w=1600&q=60') center/cover no-repeat;
      opacity: .15;
    }
    .hero-inner { position: relative; z-index: 1; }
    .hero-eye {
      font-size: .68rem;
      letter-spacing: .5em;
      color: var(--gold-l);
      text-transform: uppercase;
      margin-bottom: 1rem;
    }
    .hero-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(2rem, 5vw, 4rem);
      font-weight: 300;
      letter-spacing: .12em;
      line-height: 1.3;
    }
    .hero-line {
      width: 44px; height: 1px;
      background: var(--gold);
      margin: 1.8rem auto;
    }
    .hero-desc {
      font-size: .82rem;
      line-height: 2;
      color: rgba(255,255,255,.55);
      letter-spacing: .08em;
    }

    /* YEN SECTION */
    .yen-section {
      background: #1a1510;
      color: #fff;
      padding: 4rem 1.5rem;
      text-align: center;
    }
    .yen-eye {
      font-size: .65rem;
      letter-spacing: .45em;
      color: var(--gold-l);
      text-transform: uppercase;
      margin-bottom: .8rem;
    }
    .yen-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(1.6rem, 3.5vw, 2.8rem);
      font-weight: 300;
      letter-spacing: .1em;
      margin-bottom: 1.2rem;
    }
    .yen-body {
      max-width: 560px;
      margin: 0 auto 2.5rem;
      font-size: .88rem;
      line-height: 2;
      color: rgba(255,255,255,.5);
      letter-spacing: .04em;
    }
    .rate-cards {
      display: flex;
      justify-content: center;
      gap: 1rem;
      flex-wrap: wrap;
      margin-bottom: 2.5rem;
    }
    .rate-card {
      border: 1px solid rgba(184,150,62,.25);
      border-radius: 4px;
      padding: 1.2rem 1.5rem;
      min-width: 130px;
      text-align: center;
    }
    .rate-flag { font-size: 1.6rem; margin-bottom: .3rem; }
    .rate-cur { font-size: .6rem; letter-spacing: .3em; color: rgba(255,255,255,.4); margin-bottom: .4rem; }
    .rate-val {
      font-family: 'Cormorant Garamond', serif;
      font-size: 1.7rem;
      color: #fff;
    }
    .rate-badge {
      font-size: .62rem;
      color: var(--gold-l);
      margin-top: .4rem;
      letter-spacing: .06em;
    }
    .calc-box {
      max-width: 520px;
      margin: 0 auto;
      border: 1px solid rgba(184,150,62,.25);
      border-radius: 4px;
      padding: 1.8rem;
      text-align: left;
    }
    .calc-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: 1.2rem;
      font-weight: 300;
      letter-spacing: .1em;
      margin-bottom: 1rem;
    }
    .calc-row { display: flex; gap: .75rem; margin-bottom: 1.2rem; flex-wrap: wrap; }
    .calc-input {
      flex: 1;
      min-width: 120px;
      background: rgba(255,255,255,.06);
      border: 1px solid rgba(184,150,62,.3);
      color: #fff;
      padding: .6rem .8rem;
      font-size: .9rem;
      font-family: 'Cormorant Garamond', serif;
      outline: none;
      border-radius: 2px;
    }
    .calc-select {
      background: rgba(255,255,255,.06);
      border: 1px solid rgba(184,150,62,.3);
      color: #fff;
      padding: .6rem .8rem;
      font-size: .85rem;
      outline: none;
      cursor: pointer;
      border-radius: 2px;
    }
    .calc-result {
      display: flex;
      align-items: center;
      gap: 1rem;
      flex-wrap: wrap;
    }
    .calc-col { flex: 1; min-width: 100px; }
    .calc-col-lbl { font-size: .58rem; letter-spacing: .2em; color: rgba(255,255,255,.35); text-transform: uppercase; margin-bottom: .3rem; }
    .calc-now { font-family: 'Cormorant Garamond', serif; font-size: 1.5rem; }
    .calc-was { font-family: 'Cormorant Garamond', serif; font-size: 1.3rem; color: rgba(255,255,255,.35); text-decoration: line-through; }
    .calc-save { font-family: 'Cormorant Garamond', serif; font-size: 1.8rem; color: #4ade80; }
    .calc-save-pct { font-size: .7rem; color: rgba(74,222,128,.7); letter-spacing: .1em; }

    /* TABS */
    .tab-nav {
      background: rgba(246,241,233,.98);
      border-bottom: 1px solid rgba(184,150,62,.2);
      position: sticky;
      top: 64px;
      z-index: 90;
    }
    .tab-list {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 1.5rem;
      display: flex;
      overflow-x: auto;
      scrollbar-width: none;
    }
    .tab-list::-webkit-scrollbar { display: none; }
    .tab-btn {
      flex-shrink: 0;
      padding: .85rem 1.5rem;
      font-size: .72rem;
      letter-spacing: .2em;
      color: rgba(18,16,14,.45);
      border: none;
      background: none;
      cursor: pointer;
      border-bottom: 2px solid transparent;
      transition: color .2s, border-color .2s;
      font-family: inherit;
      white-space: nowrap;
    }
    .tab-btn:hover { color: var(--ink); }
    .tab-btn.active { color: var(--gold); border-bottom-color: var(--gold); }

    /* PRODUCTS */
    .section-wrap {
      max-width: 1200px;
      margin: 0 auto;
      padding: 4rem 1.5rem 5rem;
    }
    .section-head {
      display: flex;
      align-items: center;
      gap: 1.2rem;
      margin-bottom: 2.5rem;
    }
    .section-lbl {
      font-size: .68rem;
      letter-spacing: .45em;
      color: var(--gold);
      text-transform: uppercase;
      white-space: nowrap;
    }
    .section-rule {
      flex: 1;
      height: 1px;
      background: linear-gradient(to right, rgba(184,150,62,.4), transparent);
    }
    .product-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
      gap: 1.8rem;
    }
    .product-card {
      background: #fff;
      border: 1px solid rgba(184,150,62,.12);
      display: flex;
      flex-direction: column;
      transition: box-shadow .3s, transform .3s;
    }
    .product-card:hover {
      box-shadow: 0 16px 48px rgba(18,16,14,.1);
      transform: translateY(-4px);
    }
    .product-img-wrap {
      aspect-ratio: 4/3;
      overflow: hidden;
      background: #f0ece4;
      display: block;
      text-decoration: none;
    }
    .product-img-wrap img {
      width: 100%;
      height: 100%;
      object-fit: contain;
      padding: 1rem;
      transition: transform .5s ease;
    }
    .product-card:hover .product-img-wrap img { transform: scale(1.05); }
    .product-body {
      padding: 1.3rem;
      display: flex;
      flex-direction: column;
      flex: 1;
    }
    .product-cat {
      font-size: .58rem;
      letter-spacing: .35em;
      color: var(--gold);
      text-transform: uppercase;
      margin-bottom: .5rem;
    }
    .product-name {
      font-size: .85rem;
      line-height: 1.8;
      color: var(--ink);
      flex: 1;
      margin-bottom: .8rem;
      text-decoration: none;
    }
    .yen-badge {
      font-size: .58rem;
      padding: .2rem .6rem;
      background: rgba(74,222,128,.1);
      color: #16a34a;
      border: 1px solid rgba(74,222,128,.3);
      display: inline-block;
      margin-bottom: .8rem;
      letter-spacing: .06em;
    }
    .product-price {
      font-family: 'Cormorant Garamond', serif;
      font-size: 1.55rem;
      color: var(--ink);
      margin-bottom: 1rem;
    }
    .product-price small { font-size: .85rem; color: rgba(18,16,14,.5); }
    .btn-buy {
      display: block;
      text-align: center;
      padding: .8rem;
      background: var(--ink);
      color: #fff;
      text-decoration: none;
      font-size: .62rem;
      letter-spacing: .38em;
      text-transform: uppercase;
      transition: background .22s;
    }
    .btn-buy:hover { background: var(--gold); }

    /* PANEL */
    .tab-panel { display: none; }
    .tab-panel.active { display: block; }

    /* WISHLIST BUTTON (header) */
    .wl-header-btn {
      background: none; border: 1px solid rgba(184,150,62,.35);
      color: var(--ink); padding: .3rem .75rem;
      font-size: .7rem; letter-spacing: .1em;
      cursor: pointer; border-radius: 2px;
      display: flex; align-items: center; gap: .4rem;
      font-family: inherit; transition: border-color .2s;
    }
    .wl-header-btn:hover { border-color: var(--gold); }
    .wl-count {
      background: var(--gold); color: #fff;
      font-size: .6rem; border-radius: 50%;
      width: 16px; height: 16px;
      display: none; align-items: center; justify-content: center;
    }
    .wl-count.visible { display: flex; }

    /* WISHLIST HEART on card */
    .wl-btn {
      position: absolute; top: .6rem; right: .6rem;
      background: rgba(255,255,255,.85); border: none;
      width: 32px; height: 32px; border-radius: 50%;
      font-size: 1rem; cursor: pointer; line-height: 1;
      transition: background .2s, transform .2s;
    }
    .wl-btn:hover { background: #fff; transform: scale(1.15); }
    .wl-btn.saved { color: #e63946; }

    /* DRAWER */
    .drawer-overlay {
      position: fixed; inset: 0; background: rgba(0,0,0,.4);
      z-index: 200; opacity: 0; pointer-events: none;
      transition: opacity .3s;
    }
    .drawer-overlay.open { opacity: 1; pointer-events: all; }
    .drawer {
      position: fixed; top: 0; right: 0; bottom: 0;
      width: min(380px, 92vw);
      background: var(--washi);
      z-index: 201;
      transform: translateX(100%);
      transition: transform .35s cubic-bezier(.4,0,.2,1);
      display: flex; flex-direction: column;
      box-shadow: -8px 0 32px rgba(0,0,0,.12);
    }
    .drawer.open { transform: translateX(0); }
    .drawer-head {
      padding: 1.2rem 1.5rem;
      border-bottom: 1px solid rgba(184,150,62,.2);
      display: flex; align-items: center; justify-content: space-between;
    }
    .drawer-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: 1.2rem; font-weight: 300; letter-spacing: .1em;
    }
    .drawer-close {
      background: none; border: none; font-size: 1.4rem;
      cursor: pointer; color: rgba(18,16,14,.4);
      padding: 0 .3rem; line-height: 1;
    }
    .drawer-body { flex: 1; overflow-y: auto; padding: 1rem 1.5rem; }
    .wl-empty {
      text-align: center; padding: 3rem 0;
      color: rgba(18,16,14,.3); font-size: .85rem; letter-spacing: .1em;
    }
    .wl-item {
      display: flex; gap: 1rem; align-items: center;
      padding: .9rem 0; border-bottom: 1px solid rgba(184,150,62,.12);
    }
    .wl-item-img {
      width: 60px; height: 60px; object-fit: contain;
      background: #f0ece4; padding: .3rem; flex-shrink: 0;
    }
    .wl-item-info { flex: 1; }
    .wl-item-name { font-size: .8rem; line-height: 1.5; margin-bottom: .3rem; }
    .wl-item-price {
      font-family: 'Cormorant Garamond', serif;
      font-size: 1.1rem; color: var(--ink);
    }
    .wl-item-remove {
      background: none; border: none; color: rgba(18,16,14,.3);
      font-size: 1.1rem; cursor: pointer; padding: 0 .3rem;
    }
    .wl-item-remove:hover { color: #e63946; }
    .drawer-footer { padding: 1rem 1.5rem; border-top: 1px solid rgba(184,150,62,.2); }
    .btn-wl-clear {
      width: 100%; padding: .75rem; border: 1px solid rgba(184,150,62,.3);
      background: none; color: rgba(18,16,14,.45); font-family: inherit;
      font-size: .65rem; letter-spacing: .25em; cursor: pointer;
      text-transform: uppercase; transition: all .2s;
    }
    .btn-wl-clear:hover { border-color: var(--gold); color: var(--ink); }

    /* EMAIL SECTION */
    .email-section {
      background: var(--ink); color: #fff;
      padding: 4rem 1.5rem; text-align: center;
    }
    .email-eye {
      font-size: .62rem; letter-spacing: .45em;
      color: var(--gold-l); text-transform: uppercase;
      margin-bottom: .8rem;
    }
    .email-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(1.4rem, 3vw, 2.2rem);
      font-weight: 300; letter-spacing: .1em; margin-bottom: .8rem;
    }
    .email-desc {
      font-size: .8rem; color: rgba(255,255,255,.45);
      letter-spacing: .06em; margin-bottom: 2rem; line-height: 1.8;
    }
    .email-form {
      display: flex; gap: .75rem; justify-content: center;
      flex-wrap: wrap; max-width: 480px; margin: 0 auto;
    }
    .email-input {
      flex: 1; min-width: 220px;
      background: rgba(255,255,255,.07);
      border: 1px solid rgba(184,150,62,.3);
      color: #fff; padding: .75rem 1rem;
      font-size: .85rem; font-family: inherit;
      outline: none; border-radius: 2px;
    }
    .email-input::placeholder { color: rgba(255,255,255,.25); }
    .email-input:focus { border-color: var(--gold-l); }
    .btn-email {
      padding: .75rem 1.6rem;
      background: var(--gold); color: #fff; border: none;
      font-size: .65rem; letter-spacing: .3em; text-transform: uppercase;
      cursor: pointer; font-family: inherit; border-radius: 2px;
      transition: background .2s; white-space: nowrap;
    }
    .btn-email:hover { background: var(--gold-l); }
    .email-success {
      display: none; color: #4ade80;
      font-size: .8rem; letter-spacing: .1em; margin-top: 1rem;
    }

    /* CATEGORY GRID */
    .cat-section {
      background: var(--washi);
      padding: 4rem 1.5rem 5rem;
    }
    .cat-inner {
      max-width: 1200px;
      margin: 0 auto;
    }
    .cat-eye {
      font-size: .65rem;
      letter-spacing: .45em;
      color: var(--gold);
      text-transform: uppercase;
      text-align: center;
      margin-bottom: .6rem;
    }
    .cat-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(1.4rem, 3vw, 2.2rem);
      font-weight: 300;
      letter-spacing: .1em;
      text-align: center;
      margin-bottom: 2.5rem;
      color: var(--ink);
    }
    .cat-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
      gap: 1rem;
    }
    .cat-card {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: .6rem;
      padding: 1.6rem 1rem;
      border: 1px solid rgba(184,150,62,.18);
      background: #fff;
      text-decoration: none;
      color: var(--ink);
      transition: box-shadow .25s, transform .25s, border-color .25s;
      border-radius: 2px;
    }
    .cat-card:hover {
      box-shadow: 0 8px 28px rgba(18,16,14,.09);
      transform: translateY(-3px);
      border-color: rgba(184,150,62,.45);
    }
    .cat-icon {
      width: 2.4rem;
      height: 2.4rem;
      display: flex;
      align-items: center;
      justify-content: center;
      color: rgba(18,16,14,.5);
      transition: color .25s ease;
    }
    .cat-card:hover .cat-icon { color: var(--gold); }
    .cat-name {
      font-size: .78rem;
      font-weight: 500;
      letter-spacing: .06em;
      text-align: center;
      color: var(--ink);
    }
    .cat-desc {
      font-size: .62rem;
      color: rgba(18,16,14,.4);
      letter-spacing: .04em;
      text-align: center;
      line-height: 1.5;
    }
    .cat-arrow {
      font-size: .65rem;
      color: var(--gold);
      letter-spacing: .1em;
      margin-top: .2rem;
    }

    /* FOOTER */
    footer {
      border-top: 1px solid rgba(184,150,62,.18);
      padding: 2.2rem 1.5rem;
      text-align: center;
      font-size: .62rem;
      letter-spacing: .35em;
      color: rgba(18,16,14,.3);
      text-transform: uppercase;
    }
  </style>
</head>
<body>

  <!-- FOREX TICKER -->
  <div class="ticker">
    <span class="ticker-lbl">LIVE RATE</span>
    <span>USD <span class="ticker-rate" id="t-usd">…</span></span>
    <span>EUR <span class="ticker-rate" id="t-eur">…</span></span>
    <span>GBP <span class="ticker-rate" id="t-gbp">…</span></span>
    <span>AUD <span class="ticker-rate" id="t-aud">…</span></span>
    <span class="ticker-save" id="t-save">Live JPY Rate</span>
  </div>

  <!-- HEADER -->
  <header>
    <div class="header-inner">
      <div class="logo">Japan<em>Daily</em> Hub</div>
      <div style="display:flex;gap:1.5rem;align-items:center;">
        <a href="/japandaily-hub/about/" style="font-size:.62rem;letter-spacing:.22em;text-transform:uppercase;color:rgba(18,16,14,.5);text-decoration:none;transition:color .2s;" onmouseover="this.style.color='#b8963e'" onmouseout="this.style.color='rgba(18,16,14,.5)'">About</a>
        <button class="wl-header-btn" id="wl-open-btn" aria-label="Wishlist">
          ♡ <span class="wl-count" id="wl-count">0</span>
        </button>
        <select class="lang-select" id="lang-select">
        <option value="en">🌐 English</option>
        <option value="ja">🇯🇵 日本語</option>
        <option value="zh">🇨🇳 简体中文</option>
        <option value="zhtw">🇹🇼 繁體中文</option>
        <option value="ko">🇰🇷 한국어</option>
        <option value="fr">🇫🇷 Français</option>
        <option value="de">🇩🇪 Deutsch</option>
        <option value="es">🇪🇸 Español</option>
      </select>
      </div>
    </div>
  </header>

  <!-- HERO -->
  <section class="hero">
    <div class="hero-bg"></div>
    <div class="hero-inner">
      <p class="hero-eye"
        data-en="Curated from Japan" data-ja="日本から厳選"
        data-zh="日本精选" data-zhtw="日本精選"
        data-ko="일본에서 엄선" data-fr="Sélectionné du Japon"
        data-de="Kuratiert aus Japan" data-es="Seleccionado de Japón">Curated from Japan</p>
      <h1 class="hero-title"
        data-en="Japanese Beauty,<br/>Into Your Life"
        data-ja="日本の美しさを、<br/>あなたの暮らしへ"
        data-zh="日本之美，<br/>融入您的生活"
        data-zhtw="日本之美，<br/>融入您的生活"
        data-ko="일본의 아름다움을<br/>당신의 삶으로"
        data-fr="La Beauté Japonaise,<br/>Dans Votre Vie"
        data-de="Japanische ScN[�nheit<br/>in Ihrem Leben"
        data-es="La Belleza Japonesa,<br/>en Tu Vida">
        Japanese Beauty,<br/>Into Your Life
      </h1>
      <div class="hero-line"></div>
      <p class="hero-desc"
        data-en="Handpicked Japanese products — where tradition meets craftsmanship."
        data-ja="伝統と職人技が交差する日本の逸品を厳選。"
        data-zh="精选日本商品——传统与工艺的完美融合。"
        data-zhtw="精選日本商品——傳統與工藝的完美融合。"
        data-ko="전통과 장인정신이 만나는 일본 상품을 엄선했습니다."
        data-fr="Des produits japonais soigneusement sélectionnés — où la tradition rencontre le savoir-faire."
        data-de="Handverlesene japanische Produkte — wo Tradition auf Handwerkskunst trifft."
        data-es="Productos japoneses seleccionados — donde la tradición se encuentra con la artesanía.">
        Handpicked Japanese products — where tradition meets craftsmanship.
      </p>
    </div>
  </section>

  <!-- SOURCE PRICING -->
  <section class="yen-section">
    <p class="yen-eye"
      data-en="Source Intelligence" data-ja="産地直送の価値"
      data-zh="产地直送" data-zhtw="產地直送"
      data-ko="산지 직배송" data-fr="Intelligence Marché"
      data-de="Direktbezug" data-es="Precios de Origen">Source Intelligence</p>
    <h2 class="yen-title"
      data-en="Japanese Market Price. Delivered Worldwide."
      data-ja="日本の市場価格で。世界へ。"
      data-zh="日本市场价格。全球配送。" data-zhtw="日本市場價格。全球配送。"
      data-ko="일본 현지 가격. 전 세계 배송." data-fr="Prix japonais. Livraison mondiale."
      data-de="Japanischer Preis. Weltweit geliefert." data-es="Precio japonés. Entrega mundial.">Japanese Market Price. Delivered Worldwide.</h2>
    <p class="yen-body"
      data-en="The same products sold abroad at a 2–3× import premium — available here at authentic Japanese retail price. Not a sale. Just the source."
      data-ja="海外では2〜3倍の輸入価格がつく商品も、ここでは日本の正規小売価格でお届けします。セールではなく、産地直送です。"
      data-zh="在国外以2-3倍进口溢价出售的商品——在这里以正宗的日本零售价提供。不是促销，而是直接从源头获取。"
      data-zhtw="在國外以2-3倍進口溢價出售的商品——在這裡以正宗的日本零售價提供。不是促銷，而是直接從源頭獲取。"
      data-ko="해외에서 2~3배의 수입 프리미엄으로 판매되는 제품들 — 여기서는 일본 현지 소매 가격으로 제공됩니다."
      data-fr="Les mêmes produits vendus à l'étranger avec une prime d'importation de 2 à 3× — disponibles ici au prix de détail japonais authentique."
      data-de="Dieselben Produkte, die im Ausland mit einem 2–3-fachen Aufschlag verkauft werden — hier zum authentischen japanischen Einzelhandelspreis."
      data-es="Los mismos productos vendidos en el extranjero con un recargo de 2–3× — disponibles aquí al precio minorista japonés auténtico.">The same products sold abroad at a 2–3× import premium — available here at authentic Japanese retail price. Not a sale. Just the source.</p>
    <div class="rate-cards">
      <div class="rate-card"><div class="rate-flag">🇺🇸</div><div class="rate-cur">USD</div><div class="rate-val" id="r-usd">…</div><div class="rate-badge" id="b-usd"></div></div>
      <div class="rate-card"><div class="rate-flag">🇪🇺</div><div class="rate-cur">EUR</div><div class="rate-val" id="r-eur">…</div><div class="rate-badge" id="b-eur"></div></div>
      <div class="rate-card"><div class="rate-flag">🇬🇧</div><div class="rate-cur">GBP</div><div class="rate-val" id="r-gbp">…</div><div class="rate-badge" id="b-gbp"></div></div>
      <div class="rate-card"><div class="rate-flag">🇦🇺</div><div class="rate-cur">AUD</div><div class="rate-val" id="r-aud">…</div><div class="rate-badge" id="b-aud"></div></div>
    </div>
  </section>




  <!-- WISHLIST DRAWER -->
  <div class="drawer-overlay" id="wl-overlay"></div>
  <div class="drawer" id="wl-drawer" role="dialog" aria-label="Wishlist">
    <div class="drawer-head">
      <div class="drawer-title"
        data-en="My Wishlist" data-ja="ウィッシュリスト"
        data-zh="我的心愿单" data-zhtw="我的心願單"
        data-ko="위시리스트" data-fr="Ma Liste de Souhaits"
        data-de="Wunschliste" data-es="Mi Lista de Deseos">My Wishlist</div>
      <button class="drawer-close" id="wl-close-btn" aria-label="Close">✕</button>
    </div>
    <div class="drawer-body" id="wl-body">
      <div class="wl-empty"
        data-en="Your wishlist is empty" data-ja="リストは空です"
        data-zh="心愿单为空" data-zhtw="心願單為空"
        data-ko="위시리스트가 비어 있습니다" data-fr="Votre liste est vide"
        data-de="Ihre Wunschliste ist leer" data-es="Tu lista estˡ vacía">Your wishlist is empty</div>
    </div>
    <div class="drawer-footer">
      <button class="btn-wl-clear" id="wl-clear-btn"
        data-en="Clear all" data-ja="すべて削除"
        data-zh="清空全部" data-zhtw="清空全部"
        data-ko="전체 삭제" data-fr="Tout supprimer"
        data-de="Alles löschen" data-es="Borrar todo">Clear all</button>
    </div>
  </div>

  <!-- CATEGORY GRID -->
  <section class="cat-section">
    <div class="cat-inner">
      <p class="cat-eye"
        data-en="Shop by Category" data-ja="カテゴリから探す"
        data-zh="按类别浏览" data-zhtw="按類別瀏覽"
        data-ko="카테고리별 쇼핑" data-fr="Acheter par Catégorie"
        data-de="Nach Kategorie stöbern" data-es="Comprar por Categoría">Shop by Category</p>
      <h2 class="cat-title"
        data-en="Find What You Love" data-ja="お気に入りのカテゴリを探す"
        data-zh="找到您喜欢的" data-zhtw="找到您喜歡的"
        data-ko="마음에 드는 것을 찾아보세요" data-fr="Trouvez Ce Que Vous Aimez"
        data-de="Finden Sie, Was Sie Lieben" data-es="Encuentra Lo Que Te Gusta">Find What You Love</h2>
      <div class="cat-grid">
        {categories.map(cat => (
          <a href={`${base}/category/${cat.slug}/`} class="cat-card">
            <span class="cat-icon"><CategoryIcon path={cat.iconPath} size={2.2} /></span>
            <span class="cat-name">{cat.nameJa}</span>
            <span class="cat-desc">{cat.descJa}</span>
            <span class="cat-arrow">→</span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- EMAIL SECTION -->
  <section class="email-section">
    <p class="email-eye"
      data-en="Stay ahead of the market" data-ja="市場の動向をいち早くキャッチ"
      data-zh="抢先掌握市场动态" data-zhtw="搶先掌握市場動態"
      data-ko="시장 동향을 먼저 파악하세요" data-fr="Gardez une longueur d'avance"
      data-de="Immer einen Schritt voraus" data-es="Adelántate al mercado">Stay ahead of the market</p>
    <h2 class="email-title"
      data-en="Get Sale Alerts" data-ja="セール商品を定期的に受け取る"
      data-zh="获取特卖优惠提醒" data-zhtw="獲取特賣優惠提醒"
      data-ko="할인 알림 받기" data-fr="Recevoir les Alertes Promos"
      data-de="Angebots-Benachrichtigungen" data-es="Recibe Alertas de Ofertas">Get Sale Alerts</h2>
    <p class="email-desc"
  2   data-en="Sign up to receive limited-time deals and exclusive Japanese products directly to your inbox."
      data-ja="期間限定セールやお得な日本商品情報をメールでお届けします。"
      data-zh="订阅后定期收到限时特卖和精选日本商品信息。"
      data-zhtw="訂閱後定期收到限時特賣和精選日本商品資訊。"
      data-ko="한정 할인 및 엄선된 일본 상품 정보를 메일로 받아보세요."
      data-fr="Inscrivez-vous pour recevoir des offres limitées et des produits japonais exclusifs."
      data-de="Melden Sie sich an und erhalten Sie zeitlich begrenzte Angebote und exklusive japanische Produkte."
      data-es="Suscríbete para recibir ofertas limitadas y productos japoneses exclusivos.">
      Sign up to receive limited-time deals and exclusive Japanese products directly to your inbox.
    </p>
    <form class="email-form" id="email-form">
      <input class="email-input" id="email-input" type="email" required
        placeholder="your@email.com" autocomplete="email" />
      <button class="btn-email" type="submit"
        data-en="Notify Me" data-ja="通知を受け取る"
        data-zh="通知我" data-zhtw="通知我"
        data-ko="알림 받기" data-fr="Me Notifier"
        data-de="Benachrichtige mich" data-es="Notifícame">Notify Me</button>
    </form>
    <p class="email-success" id="email-success"
      data-en="✓ Subscribed! You'll receive sale alerts and special deals regularly."
      data-ja="✓ 登録完了！セール情報やお得な商品を定期的にお届けします。"
      data-zh="✓ 已订阅！您将定期收到特卖优惠和精选商品信息。"
      data-zhtw="✓ 已訂閱！您將定期收到特賣優惠和精選商品資訊。"
      data-ko="✓ 등록 완료! 할인 정보와 특별 상품을 정기적으로 받아보실 수 있습니다."
      data-fr="✓ Inscrit(e) ! Vous recevrez régulièrement des offres et produits exclusifs."
      data-de="✓ Angemeldet! Sie erhalten regelmäßig Angebote und exklusive Produkte."
      data-es="✓ ¡Suscrito! Recibirás ofertas y productos especiales regularmente.">
      ✓ Subscribed! You'll receive sale alerts and special deals regularly.
    </p>
  </section>

  <footer>
    &copy; 2025 Japan Daily Hub &nbsp;|&nbsp;
    <span
      data-en="Authentic Japanese Products, Delivered Worldwide"
      data-ja="日本の逸品、世界へ"
      data-zh="正宗日本商品，全球配送"
      data-zhtw="正宗日本商品，全球配送"
      data-ko="정품 일본 상품, 전 세계 배송"
      data-fr="Produits Japonais Authentiques, Livrés Partout"
      data-de="Authentische Japanische Produkte, Weltweit Geliefert"
      data-es="Productos Japoneses Auténticos, Entregados en Todo el Mundo">Authentic Japanese Products, Delivered Worldwide</span>
  </footer>

  <script is:inline>
    // ── 為替レート ──
    const BASE = { USD: 108, EUR: 128, GBP: 142, AUD: 80 };
    const SYM  = { USD: '$', EUR: '€', GBP: '£', AUD: 'A$' };
    let rates  = {};

    async function loadRates() {
      try {
        const res  = await fetch('https://open.er-api.com/v6/latest/JPY');
        const data = await res.json();
        rates = {
          USD: Math.round(1 / data.rates.USD),
          EUR: Math.round(1 / data.rates.EUR),
          GBP: Math.round(1 / data.rates.GBP),
          AUD: Math.round(1 / data.rates.AUD),
        };
        // ticker
        document.getElementById('t-usd').textContent = '¥' + rates.USD;
        document.getElementById('t-eur').textContent = '¥' + rates.EUR;
        document.getElementById('t-gbp').textContent = '¥' + rates.GBP;
        document.getElementById('t-aud').textContent = '¥' + rates.AUD;

        // rate cards
        const pct = Math.round(((rates.USD - BASE.USD) / rates.USD) * 100);
        ['USD','EUR','GBP','AUD'].forEach(c => {
          const rEl = document.getElementById('r-' + c.toLowerCase());
          const bEl = document.getElementById('b-' + c.toLowerCase());
          if (rEl) rEl.textContent = '¥' + rates[c];
          const p = Math.round(((rates[c] - BASE[c]) / rates[c]) * 100);
          if (bEl) bEl.textContent = '';
        });

        document.getElementById('t-save').textContent = 'Live rate · Updated ' + new Date().toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'});
        updateSaveBadges();
      } catch(e) {
        rates = { USD: 155, EUR: 168, GBP: 196, AUD: 100 };
        updateSaveBadges();
      }
    }

    function updateSaveBadges() {
      const usdRate = rates.USD || 155;
      document.querySelectorAll('.save-badge[data-price]').forEach(badge => {
        const price = Number(badge.dataset.price) || 0;
        if (!price) { badge.textContent = ''; return; }
        const usdNow = price / usdRate;
        const usdWas = price / 108;
        const saved  = usdWas - usdNow;
        const pct    = Math.round((saved / usdWas) * 100);
        if (pct > 0) {
          badge.textContent = '↓ $' + saved.toFixed(0) + ' vs import retail';
          badge.style.display = '';
        } else {
          badge.style.display = 'none';
        }
      });
    }


    // ── タブ (client-side filter) ──
    const TAB_LABELS = {
      all:     { en:'All Products',   ja:'すべての商品', zh:'全部商品', zhtw:'全部商品', ko:'전체 상품',    fr:'Tous les Produits',  de:'Alle Produkte',  es:'Todos los Productos' },
      food:    { en:'Food & Drink',   ja:'飲食・食品',   zh:'食品饮料', zhtw:'食品飲料', ko:'식품·음료',    fr:'Alimentation',       de:'Essen & Trinken', es:'Comida y Bebida' },
      daily:   { en:'Daily Goods',    ja:'日用雑貨',     zh:'日用品',   zhtw:'日用品',   ko:'생활용품',     fr:'Articles Quotidiens', de:'Alltagswaren',   es:'Artículos Diarios' },
      fashion: { en:'Fashion',        ja:'ファッション', zh:'时尚',     zhtw:'時尚',     ko:'패션',         fr:'Mode',               de:'Mode',           es:'Moda' },
    };

    function applyTab(tab) {
      document.querySelectorAll('.tab-btn').forEach(b => b.classList.toggle('active', b.dataset.tab === tab));
      const cards = document.querySelectorAll('.product-card');
      let visible = 0;
      cards.forEach(card => {
        const show = tab === 'all' || card.dataset.tab === tab;
        card.style.display = show ? '' : 'none';
        if (show) visible++;
      });
      const lbl = document.getElementById('section-lbl-text');
      if (lbl) {
        const lang = document.getElementById('lang-select')?.value || 'en';
        lbl.textContent = TAB_LABELS[tab]?.[lang] || TAB_LABELS[tab]?.en || tab;
      }
      const cnt = document.getElementById('product-count');
      if (cnt) cnt.textContent = visible + ' items';
    }

    document.querySelectorAll('.tab-btn').forEach(btn => {
      btn.addEventListener('click', () => applyTab(btn.dataset.tab));
    });
    applyTab('all');

    // ── 言語切替 ──
    const LANGS = ['en','ja','zh','zhtw','ko','fr','de','es'];

    function setLang(lang) {
      if (!LANGS.includes(lang)) lang = 'en';
      document.querySelectorAll('[data-en]').forEach(el => {
        const text = el.dataset[lang] || el.dataset['en'];
        if (text) el.innerHTML = text;
      });
      const sel = document.getElementById('lang-select');
      if (sel) sel.value = lang;
      try { localStorage.setItem('jdh-lang', lang); } catch(e) {}
    }

    // selectのchangeイベントをスクリプト内でバインド（Astroモジュールスコープ対応）
    const langSel = document.getElementById('lang-select');
    if (langSel) langSel.addEventListener('change', (e) => setLang(e.target.value));

    // ページ読み込み時に保存済み言語を復元
    (function() {
      try {
        const saved = localStorage.getItem('jdh-lang');
        if (saved && LANGS.includes(saved)) setLang(saved);
      } catch(e) {}
    })();

    loadRates();
    setInterval(loadRates, 5 * 60 * 1000);

    // ── ウィッシュリスト ──
    const WL_KEY = 'jdh-wishlist';

    function getWL() {
      try { return JSON.parse(localStorage.getItem(WL_KEY) || '[]'); } catch { return []; }
    }
    function saveWL(list) {
      try { localStorage.setItem(WL_KEY, JSON.stringify(list)); } catch {}
    }

    function renderWLCount() {
      const list = getWL();
      const countEl = document.getElementById('wl-count');
      if (!countEl) return;
      countEl.textContent = list.length;
      countEl.classList.toggle('visible', list.length > 0);
    }

    function renderWLBody() {
      const list = getWL();
      const body = document.getElementById('wl-body');
      if (!body) return;
      if (list.length === 0) {
        body.innerHTML = '<div class="wl-empty">Your wishlist is empty</div>';
        return;
      }
      body.innerHTML = list.map(item => `
        <div class="wl-item">
          <img class="wl-item-img" src="${item.img || 'https://images.unsplash.com/photo-1528360983277-13d401cdc186?w=120&q=60'}" alt="${item.title}" loading="lazy" />
          <div class="wl-item-info">
            <div class="wl-item-name">${item.title}</div>
            <div class="wl-item-price">&#165;${Number(item.price).toLocaleString()}</div>
          </div>
          <button class="wl-item-remove" data-id="${item.id}" aria-label="Remove">&#215;</button>
        </div>
      `).join('');
      body.querySelectorAll('.wl-item-remove').forEach(btn => {
        btn.addEventListener('click', () => {
          saveWL(getWL().filter(i => i.id !== btn.dataset.id));
          renderWLBody(); renderWLCount(); syncWLButtons();
        });
      });
    }

    function syncWLButtons() {
      const ids = new Set(getWL().map(i => i.id));
      document.querySelectorAll('.wl-btn').forEach(btn => {
        const saved = ids.has(btn.dataset.id);
        btn.classList.toggle('saved', saved);
        btn.textContent = saved ? '♥' : '♡';
      });
    }

    function openDrawer() {
      renderWLBody();
      document.getElementById('wl-drawer')?.classList.add('open');
      document.getElementById('wl-overlay')?.classList.add('open');
      document.body.style.overflow = 'hidden';
    }
    function closeDrawer() {
      document.getElementById('wl-drawer')?.classList.remove('open');
      document.getElementById('wl-overlay')?.classList.remove('open');
      document.body.style.overflow = '';
    }

    document.getElementById('wl-open-btn')?.addEventListener('click', openDrawer);
    document.getElementById('wl-close-btn')?.addEventListener('click', closeDrawer);
    document.getElementById('wl-overlay')?.addEventListener('click', closeDrawer);
    document.getElementById('wl-clear-btn')?.addEventListener('click', () => {
      saveWL([]); renderWLBody(); renderWLCount(); syncWLButtons();
    });

    document.querySelectorAll('.wl-btn').forEach(btn => {
      btn.addEventListener('click', () => {
        const { id, title, price, img } = btn.dataset;
        let list = getWL();
        const exists = list.some(i => i.id === id);
        saveWL(exists ? list.filter(i => i.id !== id) : [...list, { id, title, price, img }]);
        renderWLCount(); syncWLButtons();
      });
    });

    renderWLCount(); syncWLButtons();

    // ── メール登録 ──
    document.getElementById('email-form')?.addEventListener('submit', (e) => {
      e.preventDefault();
      const email = document.getElementById('email-input')?.value?.trim();
      if (!email) return;
      try {
        const subs = JSON.parse(localStorage.getItem('jdh-subscribers') || '[]');
        if (!subs.includes(email)) { subs.push(email); localStorage.setItem('jdh-subscribers', JSON.stringify(subs)); }
      } catch {}
      const form = document.getElementById('email-form');
      const success = document.getElementById('email-success');
      if (form) form.style.display = 'none';
      if (success) success.style.display = 'block';
    });
  </script>

</body>
</html>
          